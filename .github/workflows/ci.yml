name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential clang-format

      - name: Check code formatting
        run: |
          make format
          git diff --exit-code || (echo "Code is not formatted. Run 'make format' locally." && exit 1)

      - name: Build
        run: make

      - name: Smoke test (show usage)
        run: ./imagewty-tool || true

      - name: Build with sanitizers
        run: make debug

      - name: Smoke test (sanitized build)
        run: ./imagewty-tool || true
